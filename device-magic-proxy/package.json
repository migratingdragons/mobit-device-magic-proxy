{
    "name": "mobit-device-magic-proxy",
    "version": "1.0.0",
    "description": "Device Magic Proxy for Mobit",
    "main": "dist/app.js",
    "repository": "https://github.com/your-repo/mobit-device-magic-proxy",
    "author": "Your Name",
    "license": "MIT",
    "dependencies": {
        "axios": "^0.21.1"
    },
    "scripts": {
        "build": "tsc",
        "start": "node dist/app.js",
        "dev": "ts-node device-magic-proxy/app.ts",
        "test": "mocha -r ts-node/register tests/unit/**/*.ts",
        "lint": "tsc --noEmit",
        "deploy": "sam build && sam deploy --guided --profile mobit",
        "deploy:quick": "sam build && sam deploy --profile mobit",
        "clean": "rm -rf dist",
        "build:all": "npm run clean && npm run build && sam build"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.92",
        "@types/chai": "^4.3.0",
        "@types/mocha": "^9.1.0",
        "@types/node": "^17.0.21",
        "@types/sinon": "^10.0.11",
        "chai": "^4.3.6",
        "mocha": "^10.1.0",
        "sinon": "^15.0.1",
        "ts-node": "^10.7.0",
        "typescript": "^4.9.5"
    }
}
